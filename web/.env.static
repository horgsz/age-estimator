# Loaded by `vite --mode static` / `vite build --mode static`.
#
# The GitHub Pages deployment has no API to talk to, so the static build runs
# the whole inference path in the browser. The default `vite build` is
# unchanged and still targets the FastAPI server, which is what `make dev` uses.
VITE_ENGINE=browser
