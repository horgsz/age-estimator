fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9
opencv-python-headless>=4.10,<5.0
numpy>=1.26,<3.0
torch>=2.3
timm>=1.0

# dev / test
pytest>=8.0
httpx>=0.27
# Used by server/tools/export_static_registry.py to prove that each ONNX export
# really is the checkpoint whose accuracy figures it is about to carry, and by
# server/tests/test_static_registry.py to keep the committed copy honest. Not
# used at serving time -- the API runs the .pt checkpoints through torch.
onnxruntime>=1.17
